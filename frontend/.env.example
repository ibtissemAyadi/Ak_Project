# Copier ce fichier en .env (local) et NE JAMAIS committer les valeurs réelles.
# Sur Vercel : configurer cette variable dans Project Settings > Environment Variables.

# URL de base de l'API backend (Render en production).
# Ex. production : https://gestion-ak-backend.onrender.com
# Ex. local       : http://127.0.0.1:8000
VITE_API_URL=
